# Copy to .env for local overrides (optional — defaults to the local backend).
# In production (Vercel), set this to your deployed backend + /api/v1:
#   VITE_API_URL=https://your-api.onrender.com/api/v1
VITE_API_URL=http://localhost:5000/api/v1
